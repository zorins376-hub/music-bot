# Minimal dependencies for the standalone crawler service
# Connects to existing Supabase PostgreSQL + Redis — no Telegram, no ffmpeg, no yt-dlp

# Core
pydantic-settings==2.13.1
sqlalchemy[asyncio]==2.0.48
psycopg[binary]>=3.1.0

# Redis (crawler queue state)
redis[asyncio]==7.3.0

# Music APIs
yandex-music==2.2.0
spotipy==2.26.0

# HTTP
aiohttp==3.10.11
