# ── Core bot (MVP) ────────────────────────────────────────────────────────
aiogram==3.15.0
aiohttp>=3.9.0
aiofiles>=23.2.0
pydantic-settings>=2.0.0

# ── Download / audio ──────────────────────────────────────────────────────
yt-dlp>=2026.3.3
mutagen>=1.47.0
pydub>=0.25.0

# ── Database ──────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0

# ── Cache ─────────────────────────────────────────────────────────────────
redis[asyncio]>=5.0.0
fakeredis>=2.0.0
# ── Music APIs ────────────────────────────────────────────
yandex-music>=2.0.0
spotipy>=2.23.0

# ── Recognition ───────────────────────────────────────────
shazamio>=0.4.0

# ── VK Music ─────────────────────────────────────────────
vk_api

# ── Monitoring ────────────────────────────────────────────
sentry-sdk[asyncio]>=1.40.0
prometheus-client>=0.20.0

# ── Utilities ─────────────────────────────────────────────
packaging>=23.0

# ── Story cards / QR ──────────────────────────────────────
Pillow>=10.0.0
qrcode[pil]>=7.0

# ── TTS (Voice AI DJ) ────────────────────────────────────
edge-tts>=6.1.0
gTTS>=2.4.0

# ── Import (Apple Music scraping) ─────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── TMA Player (FastAPI backend) ─────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# ── Event loop acceleration (Linux only) ─────────────────
uvloop>=0.19.0; sys_platform != "win32"

# ── Fuzzy matching ────────────────────────────────────────
rapidfuzz>=3.9.0

# ── ML Recommendations (optional — only needed when SUPABASE_AI_ENABLED=False)
# Install separately: pip install numpy>=1.26.0 implicit>=0.7.0 scipy>=1.11.0 gensim>=4.3.0